{
  "name": "imejii",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Bildbetrachter mit Werkzeugkasten: ansehen, bearbeiten, konvertieren und freistellen - alles lokal.",
  "author": {
    "name": "Frederik Morbe"
  },
  "scripts": {
    "dev": "vite",
    "predev": "node scripts/prepare-codecs.mjs",
    "prebuild": "node scripts/prepare-codecs.mjs",
    "pretest:renderer": "node scripts/prepare-codecs.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "desktop": "node scripts/electron-dev.mjs",
    "predesktop": "node scripts/prepare-codecs.mjs",
    "desktop:preview": "npm run build && electron .",
    "electron": "electron .",
    "lint": "eslint . --max-warnings=0",
    "test:unit": "node --test tests/*.test.cjs",
    "test:renderer": "electron tests/run-renderer.cjs",
    "test:desktop": "electron tests/desktop-smoke.cjs",
    "test": "npm run lint && npm run test:unit && npm run build && npm run test:renderer && npm run test:desktop",
    "build:assets": "electron scripts/build-assets.cjs && node scripts/third-party-notices.mjs",
    "pack:win": "npm run build && npm run build:assets && electron-builder --config electron-builder.config.cjs --win --x64 --dir --publish never",
    "dist:win": "npm test && npm run build:assets && electron-builder --config electron-builder.config.cjs --win --x64 --publish never",
    "dist:win:signed": "npm test && npm run build:assets && electron-builder --config electron-builder.config.cjs --win --x64 --publish never --config.forceCodeSigning=true",
    "verify:package": "node scripts/verify-package.mjs"
  },
  "dependencies": {
    "exifr": "7.1.3",
    "heic-to": "1.5.2",
    "jszip": "^3.10.1",
    "pinia": "^3.0.3",
    "utif2": "4.1.0",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@electron/fuses": "2.1.3",
    "@eslint/js": "10.0.1",
    "@vitejs/plugin-vue": "^6.0.0",
    "electron": "^44.1.1",
    "electron-builder": "26.15.3",
    "eslint": "10.9.1",
    "eslint-plugin-vue": "10.10.0",
    "globals": "17.12.0",
    "vite": "^7.0.0"
  },
  "main": "electron/main.cjs",
  "engines": {
    "node": ">=24.0.0"
  }
}
